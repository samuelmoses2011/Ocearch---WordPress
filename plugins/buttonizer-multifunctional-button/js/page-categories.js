/**
 * Initialize the conditional logic
 * @param rowobject
 */
// Init new categories parameter inside the `buttonizer` object
buttonizer.categories = {
    settings: {
        firstInit: true,
        isSaving: false
    }
};

/* Premium Code Stripped by Freemius */
